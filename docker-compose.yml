version: "3"

services:
  web:
    image: webpack
    build: .
    volumes:
      - .:/app
      - /app/node_modules # Sharing code between the container and the host file system shouldn't apply to node_modules.
    ports:
      - "8080:8080"
    command: npm start
